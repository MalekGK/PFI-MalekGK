@model Models.Course
<div class="detailsCard">
    <div class="courseMainTitle">@Model.Code</div>
    <div class="courseSubTitle">Session @Model.Session</div>
    <div class="courseSubTitle" style="font-size:18px; margin-top:4px;">@Model.Title</div>
</div>
